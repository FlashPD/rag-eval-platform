aws_region                    = "us-east-1"
github_repository             = "your-github-user/rag-eval-platform"
github_deployment_environment = "production"

# Set this only when the AWS account already has GitHub's OIDC provider.
# create_github_oidc_provider = false
